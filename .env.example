# AgentMind does not require environment variables for local development.
# This file documents optional configuration.

# Enable debug logging for hooks
# AGENTMIND_DEBUG=1

# npm publish token (used by CI only, not needed locally)
# NPM_TOKEN=
